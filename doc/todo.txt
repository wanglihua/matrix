
* revel反转路由生成的url问题，revel.template ReverseUrl
* xorm 加事务时SQL的显示问题
* 用户授权的实现
* 列表页数据复杂查询面板

* (碰到的时候再调)表单两列编排时，验证显示的问题
* (有时间时考虑这个问题，但最后也不能忘了)SQL注入攻击和跨域攻击的防范
* (做图标功能是记得用这个就行)图表使用 echarts， http://echarts.baidu.com/ BSD授权
* (有时间时考虑这个问题)数据表格，浏览器大小变动后，自动调整大小的问题
* (这个问题先不管了，前端验证，加后端验证吧，如果一个int没有绑定成功，前后端都会报错)绑定前的验证问题 不能转成int的输入数据
